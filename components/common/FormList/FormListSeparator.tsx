import { View } from "react-native";
import { colors } from "../../../lib/theme";

const FormListSeparator = () => {
  return <View style={{ flex: 1, backgroundColor: colors.gray, height: 1 }} />;
};

export default FormListSeparator;
